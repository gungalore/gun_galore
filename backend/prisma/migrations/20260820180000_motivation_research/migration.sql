-- Background research for the writer: area crime context, firearm and
-- cartridge background, gathered by web search once per motivation and
-- reused across gate cycles. Encrypted — it names the applicant's suburb.
ALTER TABLE "Motivation" ADD COLUMN IF NOT EXISTS "researchEncrypted" TEXT;
